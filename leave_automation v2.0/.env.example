# Flask runtime
FLASK_ENV = development
PORT = 5000

# OpenAI
OPENAI_API_KEY = sk-REPLACE_ME

# Slack incoming webhook (if you use Slack)
SLACK_WEBHOOK_URL = https://hooks.slack.com/services/REPLACE/REPLACE/REPLACE

# Google Sheets (service account JSON path and spreadsheet id)
SHEETS_CREDENTIALS_JSON_PATH = E:/full/path/to/credentials.json
SHEETS_SPREADSHEET_ID = YOUR_SPREADSHEET_ID_HERE

# MANAGER_SLACK_ID = @manager_username

# n8n: the URL n8n will use to call Flask (not needed by Flask, but helpful if you test reverse)
# For the forwarder pattern, n8n posts to Flask at http://host:5000/webhook — set in n8n.
